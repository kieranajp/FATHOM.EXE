// Glitch shader — stub. T-glitch ticket fills this in.
// Belongs to the legendary galleon: pixelation at silhouette edges, vertex jumps,
// solid-flat colour in a wireframe world. Ontological violation, not colour swap.

shader_type spatial;
render_mode unshaded;

void fragment() {
	ALBEDO = vec3(1.0, 0.0, 1.0);  // placeholder magenta = "unfilled shader"
}
