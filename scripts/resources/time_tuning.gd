# TimeTuning — numbers for the in-world clock.
# Authored in data/tuning/time.tres; loaded by autoload/time_system.gd (T11).
class_name TimeTuning extends Tuning

@export var seconds_per_in_world_minute: float = 1.0
